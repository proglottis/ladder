<h1><%= @tournament.name %></h1>

<% unless @tournament.has_user? current_user %>
  <p>You are not ranked in this tournament, <%= link_to "join now", join_tournament_path(@tournament), :method => :post %>.</p>
<% end %>

<ul class="nav">
  <li><%= link_to "Invite player", new_tournament_invite_path(@tournament) %></li>
</ul>

<table class="table">
  <thead>
    <tr>
      <th>Player</th>
      <th>Rank</th>
    </tr>
  </thead>
  <tbody>
    <% @tournament.ranks.each do |rank| %>
      <tr>
        <td><%= rank.user.name %></td>
        <td><%= number_with_precision rank.rank %></td>
      </tr>
    <% end %>
  </tbody>
</table>
