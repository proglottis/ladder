<%= t '.invite_requested', :name => @invite_request.user.name, :tournament => @tournament.name %>

[<%= t 'tournaments.invite_requests.index.title' %>](<%= tournament_invite_requests_url(@tournament) %>)
